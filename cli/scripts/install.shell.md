# Shell setup

Install ShellCheck and shfmt.

On Debian/Ubuntu:

```bash
sudo apt-get update
sudo apt-get install -y shellcheck shfmt
```

On macOS (Homebrew):

```bash
brew install shellcheck shfmt
```
